# ============================================================
# TITAN AI — Python bog'liqliklari (dependencies)
# O'rnatish:  pip install -r requirements.txt
# ============================================================

# --- MetaTrader 5 (faqat Windows nativ rejim) ---
# Linux/Docker'да o'rnatilmaydi — deploy/Dockerfile buni chiqarib tashlaydi,
# uning o'rniga mt5linux ko'prigi ishlaydi (MT5_HOST env berilganда).
MetaTrader5==5.0.45

# --- MT5 Linux ko'prigi (Docker/VPS: Wine ichidagi MT5 serverга RPyC) ---
# mt5linux o'zi mos rpyc (<7,>=6) ni tortadi. Agar ulanишда RPyC protokol
# mos kelmasa — server (gmag11 image) va klient rpyc versiyasini tenglang.
# DIQQAT: bot faqat mt5linux KLIENTINI ishlatadi va u SERVER (gmag11 mt5
# konteyneri) bilan bir xil versiyada bo'lishi shart. gmag11 start.sh asl
# lucas-campagna mt5linux'ni kutadi (-w wine python.exe CLI) = 0.1.9.
# PyPI'dagi mt5linux 1.0.11/1.1.0 boshqa, mos kelmaydigan paket (1.1.0 py3.12
# kerak). Shuning uchun mt5linux==0.1.9 deploy/Dockerfile'da --no-deps bilan
# o'rnatiladi; bu yerda faqat klient uchun rpyc:
rpyc>=6,<7

# --- Ma'lumotlar tahlili ---
pandas==2.2.2
numpy==1.26.4

# --- Grafik chizish ---
mplfinance==0.12.10b0
matplotlib==3.9.0

# --- Grok (xAI) / AI — OpenAI-mos SDK orqali ---
openai==1.40.0
httpx==0.27.0

# --- Telegram bot ---
aiogram==3.10.0

# --- Sozlamalar / muhit ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.4.0

# --- Utility ---
pytz==2024.1
loguru==0.7.2

# --- Bilim bazasi (RAG) — semantik embedding, local/offline (ONNX, torch KERAK EMAS) ---
# Model birinchi ishlatishда ~130MB yuklanadi (keyin keshда). Import LAZY —
# KB_ENABLED=false bo'lса yoki paket yo'q bo'lса bot baribir ishlaydi.
fastembed==0.3.6

# --- Test (ixtiyoriy) ---
pytest==8.3.2
