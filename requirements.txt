# ============================================================
# TITAN AI — Python bog'liqliklari (dependencies)
# O'rnatish:  pip install -r requirements.txt
# ============================================================

# --- MetaTrader 5 (faqat Windows nativ rejim) ---
# Linux/Docker'да o'rnatilmaydi — deploy/Dockerfile buni chiqarib tashlaydi,
# uning o'rniga mt5linux ko'prigi ishlaydi (MT5_HOST env berilganда).
MetaTrader5==5.0.45

# --- MT5 Linux ko'prigi (Docker/VPS: Wine ichidagi MT5 serverга RPyC) ---
# mt5linux o'zi mos rpyc (<7,>=6) ni tortadi. Agar ulanишда RPyC protokol
# mos kelmasa — server (gmag11 image) va klient rpyc versiyasini tenglang.
mt5linux==1.0.11

# --- Ma'lumotlar tahlili ---
pandas==2.2.2
numpy==1.26.4

# --- Grafik chizish ---
mplfinance==0.12.10b0
matplotlib==3.9.0

# --- Grok (xAI) / AI — OpenAI-mos SDK orqali ---
openai==1.40.0
httpx==0.27.0

# --- Telegram bot ---
aiogram==3.10.0

# --- Sozlamalar / muhit ---
python-dotenv==1.0.1
pydantic==2.8.2
pydantic-settings==2.4.0

# --- Utility ---
pytz==2024.1
loguru==0.7.2

# --- Test (ixtiyoriy) ---
pytest==8.3.2
